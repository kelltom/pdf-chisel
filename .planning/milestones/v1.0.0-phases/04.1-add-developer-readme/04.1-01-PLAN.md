---
phase: 04.1-add-developer-readme
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "README.md exists at the repository root"
    - "A developer can read the README and know exactly which Node version to install via nvm-windows"
    - "A developer can clone, install, and run the dev server using only the commands listed — no guesswork"
    - "Inline warnings for Node version mismatch and Windows Defender false positives are present"
    - "Build for production command is documented"
  artifacts:
    - path: "README.md"
      provides: "Developer setup documentation"
      contains: "nvm-windows, npm install, npm run dev"
  key_links:
    - from: "README.md Prerequisites"
      to: "nvm-windows install step"
      via: "Node 22 LTS version pinned with exact nvm install command"
      pattern: "nvm install"
    - from: "README.md Run in dev mode"
      to: "npm run dev"
      via: "electron-vite dev command"
      pattern: "npm run dev"
---

<objective>
Write a developer README.md at the repository root documenting the exact prerequisites, clone/install steps, dev mode run command, and production build command for PDF Chisel on Windows.

Purpose: Any developer cloning this repo can get a working dev environment without asking questions.
Output: README.md at repository root.
</objective>

<execution_context>
@C:/Users/Kell/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Kell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04.1-add-developer-readme/04.1-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write README.md</name>
  <files>README.md</files>
  <action>
Create README.md at the repository root. Follow the CONTEXT.md decisions exactly:

**Tone & structure:** Purely technical, minimal. No fluff. 1-2 sentence project description at the top, then straight into sections.

**Sections (in this order):**
1. Project description (1-2 sentences only — what PDF Chisel is)
2. Prerequisites
3. Clone and install
4. Run in dev mode
5. Build for production

**Prerequisites section:**
- Git and Node (via nvm-windows) are the only prerequisites
- Specify Node 22 LTS as the version to install (electron-vite 5 requires `^20.19.0 || >=22.12.0`; Node 22 is the stable LTS minimum)
- Exact nvm-windows command: `nvm install 22` then `nvm use 22`
- Include inline warning (GitHub-flavored `> [!WARNING]`) immediately after the nvm install line: using a Node version outside `^20.19.0 || >=22.12.0` can cause Electron native module rebuild failures
- No editor or extension requirements
- No environment variables section

**Clone and install section:**
- `git clone` command (use placeholder URL `https://github.com/YOUR_USERNAME/pdf-chisel.git`)
- `cd pdf-chisel`
- `npm install`

**Run in dev mode section:**
- `npm run dev`
- Include inline note (GitHub-flavored `> [!NOTE]`) that unsigned Electron dev builds may trigger a Windows Defender warning — this is expected and safe to dismiss

**Build for production section:**
- `npm run build:win`
- No further explanation

**Formatting rules:**
- Use `##` for section headers (no `#` title beyond the project name at top)
- All commands in fenced code blocks with `sh` language tag
- No tables, no project structure diagram, no troubleshooting section
- No descriptions per npm script — commands only
- Do NOT reference Electron internals in prose
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('README.md','utf8'); const checks=['nvm install 22','npm install','npm run dev','npm run build:win','[!WARNING]','[!NOTE]']; const missing=checks.filter(s=>!c.includes(s)); if(missing.length){console.error('MISSING:',missing);process.exit(1);}else{console.log('README verification passed');}"</automated>
    <manual>Read README.md and confirm it reads naturally from top to bottom with no missing steps — a developer should be able to follow it cold without side-channel knowledge</manual>
  </verify>
  <done>README.md exists at repository root and passes automated content check: contains nvm install 22, npm install, npm run dev, npm run build:win, a [!WARNING] callout, and a [!NOTE] callout</done>
</task>

</tasks>

<verification>
- README.md exists at C:/Repos/pdf-chisel/README.md
- Automated node content check passes (all 6 required strings present)
- File is valid UTF-8 markdown with no broken code fences
</verification>

<success_criteria>
A developer cloning pdf-chisel on Windows can read README.md and follow the steps — install nvm-windows, install Node 22, clone, npm install, npm run dev — without needing any additional documentation. Build command for production is present. Both inline warnings (Node version mismatch and Windows Defender) are in place.
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-add-developer-readme/04.1-01-SUMMARY.md` following the summary template.
</output>
