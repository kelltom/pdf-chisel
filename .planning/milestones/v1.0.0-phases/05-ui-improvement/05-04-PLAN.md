---
phase: 05-ui-improvement
plan: 04
type: execute
wave: 3
depends_on: [05-01, 05-02, 05-03]
files_modified: []
autonomous: false
requirements: []

must_haves:
  truths:
    - "All four locked bug fixes are visually confirmed working in the running app"
    - "FileInput height is stable regardless of state"
    - "Each mode has exactly one Reset button that returns the view to a blank state including no loaded file"
    - "ConvertMode does not overflow the viewport when the results section grows"
  artifacts: []
  key_links: []
---

<objective>
Human visual verification of all Phase 5 changes in the running app. Confirms the four locked bug fixes are working as intended and no regressions were introduced by the CSS/type extraction.

Purpose: Polish phases require human eyes — behavior is the goal, not just TypeScript passing.

Output: User confirms all fixes, phase is complete.
</objective>

<execution_context>
@C:/Users/Kell/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Kell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and launch the app for verification</name>
  <files></files>
  <action>
Start the development server so the user can test the app:

```bash
cd C:/Repos/pdf-chisel && npm run dev
```

Wait for the Electron window to open. If it does not open within 30 seconds, report the dev server output.
  </action>
  <verify>
    <automated>cd C:/Repos/pdf-chisel && npm run dev</automated>
  </verify>
  <done>Electron dev window is open and the app loads without console errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify all four bug fixes and regression-check all modes</name>
  <files></files>
  <action>Human visual verification — follow the how-to-verify steps below in the running Electron app.</action>
  <verify><manual>See how-to-verify steps below</manual></verify>
  <done>User types "approved" after confirming all four fixes and regression checks pass.</done>
  <what-built>
Four bug fixes applied across Plans 01–03:
1. ResultsSummary layout/overflow — ConvertMode no longer overflows the viewport when results grow
2. Reset button consolidation — each mode has exactly one Reset button in the actions area; ResultsSummary has no Reset button
3. Reset unloads PDF — clicking Reset clears the loaded file, returning the view to blank
4. FileInput height consistency — the file input box is the same height whether empty, loading, or a file is loaded

Structural cleanup (Plan 03): shared OperationResult type, global .mode-title and .btn-primary CSS.
  </what-built>
  <how-to-verify>
Test each fix in the running Electron app:

**Fix 1 — FileInput height (Plan 02):**
1. Open any mode (Extract, Split, or Convert)
2. Observe the file input area — it should be ~80px tall with the drop zone visible
3. Load a PDF file (drag or browse)
4. Observe the file input area — it should remain approximately the same height showing the file name row
5. PASS: No layout shift between empty and loaded states

**Fix 2 — Reset button placement (Plan 01):**
1. In Extract mode: verify ONE "Reset" button appears in the actions row (next to "Extract" button)
2. Verify there is NO Reset button at the bottom of the results panel after running an extraction
3. Repeat check in Split mode and Merge mode
4. PASS: Exactly one Reset per mode view, always visible in the actions row

**Fix 3 — Reset unloads PDF (Plan 01):**
1. Load a PDF in Extract mode
2. Click Reset
3. PASS: The file input area returns to the empty drop zone state (file is unloaded)
4. Repeat in Split mode and Convert mode

**Fix 4 — ConvertMode overflow (Plan 01):**
1. Load a multi-page PDF (10+ pages recommended) in Convert mode
2. Click Convert
3. After conversion completes, observe the results section with the file list
4. PASS: The results section scrolls within the page — it does NOT extend beyond the app window or cause the overall app to scroll in a broken way
5. Bonus check: the Start Review workflow still works after this layout change

**Regression check — all modes still functional:**
1. Run an Extract operation — confirm it succeeds and shows the output summary
2. Run a Split operation — confirm it succeeds
3. Run a Merge operation — confirm it succeeds
4. Verify the settings page still opens via the gear icon
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
All four bug fixes confirmed working by human visual inspection.
No regressions in core PDF operations.
</verification>

<success_criteria>
User confirms "approved" after verifying all four fixes and regression checks.
Phase 5 UI Improvement is complete.
</success_criteria>

<output>
After completion, create `.planning/phases/05-ui-improvement/05-04-SUMMARY.md`
</output>
