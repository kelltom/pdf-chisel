---
phase: 05-ui-improvement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/lib/components/FileInput.svelte
autonomous: true
requirements: []

must_haves:
  truths:
    - "FileInput displays at the same visual height whether empty, loading, or a file is loaded"
    - "The drop zone and the file-info row both occupy at least 80px of vertical space"
    - "Content within both states is vertically centered within that fixed height"
    - "The layout does not shift when a file is loaded or unloaded"
  artifacts:
    - path: "src/renderer/src/lib/components/FileInput.svelte"
      provides: "Fixed-height file input with min-height: 80px on both .drop-zone and .file-info"
  key_links:
    - from: ".drop-zone CSS"
      to: "consistent height"
      via: "min-height: 80px"
      pattern: "min-height: 80px"
    - from: ".file-info CSS"
      to: "consistent height"
      via: "min-height: 80px"
      pattern: "min-height: 80px"
---

<objective>
Fix FileInput height consistency. The drop zone is ~130px tall when empty but collapses to ~48px when a file is loaded. The layout shift is visually disruptive. Both states should occupy the same height at all times.

Purpose: "FileInput should feel stable and fixed" — per locked user decision.

Output: FileInput.svelte with min-height: 80px applied to both .drop-zone and .file-info, making the component height-stable regardless of state.
</objective>

<execution_context>
@C:/Users/Kell/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Kell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-ui-improvement/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply consistent min-height to FileInput drop zone and file info states</name>
  <files>
    src/renderer/src/lib/components/FileInput.svelte
  </files>
  <action>
In `FileInput.svelte`, update the `<style>` block only. No logic changes needed.

**Change 1 — Add min-height to .drop-zone:**

Current:
```css
.drop-zone {
  border: 2px dashed var(--color-surface-2);
  border-radius: 10px;
  padding: 32px 24px;
  display: flex;
  align-items: center;
  justify-content: center;
  background: var(--color-surface);
  transition: border-color 0.15s, background 0.15s;
  cursor: default;
}
```

Updated (add `min-height: 80px` and reduce padding to prevent over-height):
```css
.drop-zone {
  border: 2px dashed var(--color-surface-2);
  border-radius: 10px;
  padding: 16px 24px;   /* reduced from 32px — min-height now controls minimum size */
  min-height: 80px;
  display: flex;
  align-items: center;
  justify-content: center;
  background: var(--color-surface);
  transition: border-color 0.15s, background 0.15s;
  cursor: default;
}
```

**Change 2 — Add min-height to .file-info:**

Current:
```css
.file-info {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 12px 16px;
  border-radius: 10px;
  background: var(--color-surface);
  border: 1px solid var(--color-surface-2);
}
```

Updated (add `min-height: 80px`):
```css
.file-info {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 12px 16px;
  border-radius: 10px;
  min-height: 80px;
  background: var(--color-surface);
  border: 1px solid var(--color-surface-2);
}
```

Both `.drop-zone` and `.file-info` already use `display: flex; align-items: center` (or `justify-content: center`), so content will be vertically centered within the min-height. The loading state (`.loading-text` inside `.drop-zone`) inherits the drop-zone height automatically.

No other changes to FileInput.svelte — only CSS.
  </action>
  <verify>
    <automated>cd C:/Repos/pdf-chisel && npx tsc --noEmit --project src/renderer/tsconfig.json 2>&1 | head -20</automated>
    <manual>None — visual verification in checkpoint plan 04</manual>
  </verify>
  <done>FileInput.svelte has min-height: 80px on both .drop-zone and .file-info. TypeScript remains clean (CSS-only change).</done>
</task>

</tasks>

<verification>
TypeScript check passes (no logic changes, so purely a sanity check).
Visual correctness verified in the human checkpoint plan 04.
</verification>

<success_criteria>
- .drop-zone has min-height: 80px
- .file-info has min-height: 80px
- Both states visually occupy the same height in the running app
- No layout shift when toggling between empty and loaded states
</success_criteria>

<output>
After completion, create `.planning/phases/05-ui-improvement/05-02-SUMMARY.md`
</output>
