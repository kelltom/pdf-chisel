---
phase: 06-convert-mode-bugfix
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified: []
autonomous: false
requirements: [CONV-03, REVW-01, REVW-02, REVW-03, REVW-04, REVW-05]

must_haves:
  truths:
    - "Human confirms review image fits in view bounds at 300 DPI with no scrollbar"
    - "Human confirms all other mode views look correct (no missing padding)"
    - "Human confirms keyboard copy-and-next still works"
  artifacts:
    - path: "src/renderer/src/lib/components/modes/ConvertMode.svelte"
      provides: "Verified bounded review layout"
      contains: "mode-view--review"
  key_links:
    - from: "human tester"
      to: "running app"
      via: "npm run dev + manual walkthrough"
      pattern: "visual verification"
---

<objective>
Human visual verification that the Convert mode review workflow bug is fixed and no regressions were introduced in other modes.

Purpose: CSS layout bugs require visual confirmation — automated builds pass even when layout is visually broken. This checkpoint gates phase completion.

Output: Phase 6 marked complete.
</objective>

<execution_context>
@C:/Users/Kell/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Kell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-convert-mode-bugfix/06-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Two CSS fixes applied in Plan 06-01:
    1. Removed `padding: 24px` from App.svelte .content (each mode is self-padded)
    2. Added `class:mode-view--review={reviewState !== 'form'}` + `.mode-view--review { overflow: hidden; height: 100%; }` to ConvertMode.svelte
  </what-built>
  <how-to-verify>
    Run: `npm run dev` in the pdf-chisel directory

    **Test 1 — Review overflow fix (primary bug)**
    1. Load any multi-page PDF in Convert mode
    2. Set DPI to 300, format PNG
    3. Click Convert and wait for completion
    4. Click "Start Review"
    EXPECTED: Image preview fills the view area. No vertical scrollbar appears on the main content region. Image scales down to fit height.
    FAIL if: A scrollbar appears alongside the image, or the image overflows beyond the bottom of the view.

    **Test 2 — Keyboard shortcuts still work**
    5. While in review mode, press Space
    EXPECTED: Image flashes briefly, position counter advances (e.g. "1 / 12" → "2 / 12")
    6. Press Enter to advance again
    EXPECTED: Same flash + advance behavior
    7. Click Back button — EXPECTED: goes back one image

    **Test 3 — Extract mode (no padding regression)**
    8. Navigate to Extract mode
    EXPECTED: Mode title and form appear with ~20px left padding (same as before the fix)
    FAIL if: Content appears flush to the left edge with no padding.

    **Test 4 — Settings mode (no padding regression)**
    9. Navigate to Settings (gear icon)
    EXPECTED: "Settings" heading and fields appear with ~20px left padding
    FAIL if: Content appears flush to the left edge.

    **Test 5 — Convert form scrollable on small window**
    10. Resize the app window to a short height (e.g. 400px)
    11. Navigate to Convert mode
    EXPECTED: Form fields (file input, format selector, DPI selector, buttons) are scrollable — no content is cut off and inaccessible
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 tests pass, or describe which test failed and what was observed</resume-signal>
</task>

</tasks>

<verification>
All 5 manual tests pass as described in the checkpoint above.
</verification>

<success_criteria>
- Test 1: Review image is bounded (no overflow scrollbar)
- Test 2: Space/Enter keyboard shortcuts advance correctly
- Test 3: Extract mode padding unchanged
- Test 4: Settings mode padding unchanged
- Test 5: Convert form scrollable at small window heights
</success_criteria>

<output>
After completion, create `.planning/phases/06-convert-mode-bugfix/06-02-SUMMARY.md`
</output>
